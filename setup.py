from setuptools import setup, find_packages

setup(
    name="flowmsm",
    version="0.1.0",
    packages=find_packages(),
    include_package_data=True,
    description="FlowMSM - Flow in ion channels modeled via MSM",
    author="Simone Furini",
    install_requires=[],
)
